export enum ErrorsEnum {
  MISSING_PARAMS = "query param is missing",
  MISSING_USER = "user not found",
  USER_ALREADY_EXIST = "user already exist",
  FORBIDDEN = "forbidden",
  PROBLEMS_RECEIVING_DATA = "Problems receiving data",
}
